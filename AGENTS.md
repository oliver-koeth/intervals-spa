# Agent guidelines

- When starting a new feature while on the `master` branch, create and use a separate feature branch first.
- Do not publish that new feature branch to GitHub Pages immediately; keep it off Pages until the feature is ready.
